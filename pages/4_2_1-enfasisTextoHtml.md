---
title: Énfasis en texto
description: Resaltado, texto de salida, texto pequeño, tachado, subíndices y superíndices.
layout: libdoc_page.liquid
permalink: /pages/4-crear-contenido/4_2-html/4_2_1-enfasisTexto/index.html
eleventyNavigation:
    key: Énfasis en texto
    parent: HTML en línea
    order: 1
tags:
    - contenido
    - html
    - énfasis
    - resaltado
    - texto de salida
    - texto pequeño
    - tachado
    - subíndice
    - superíndice
date: 2026-03-27
---

# Resaltado de texto
Esto es un fragmento de <mark>texto resaltado</mark> con la etiqueta `<mark>`.

``` html
Esto es un fragmento de <mark>texto resaltado</mark> con la etiqueta `<mark>`.
```

# Simular Salida de texto
Esto es un fragmento que simula una <samp>Salida de texto</samp> con la etiqueta `<samp>`.

``` html
Esto es un fragmento que simula una <samp>Salida de texto</samp> con la etiqueta `<samp>`.
```

# Texto pequeño
<small>Esto es un fragmento de texto pequeño</small> con la etiqueta `<small>`.

``` html
<small>Esto es un fragmento de texto pequeño</small> con la etiqueta `<small>`.
```

# Texto tachado

Esto es un fragmento <s> de texto tachado</s> con la etiqueta `<s>`.

``` html
Esto es un fragmento <s> de texto tachado</s> con la etiqueta `<s>`.
```

# Subíndice
Para poder utilizar subíndices, como en la fórmula del agua, utilizamos la etiqueta `<sub>`: **H**<sub>2</sub>**O**.

``` html
Para poder utilizar subíndices, como en la fórmula del agua, utilizamos la etiqueta `<sub>`: **H**<sub>2</sub>**O**.
```

# Superíndice
Para poder utilizar superíndices, como en la fórmula de la energía, utilizamos la etiqueta `<sup>`: **E**=**mc**<sup>2</sup>.

``` html
Para poder utilizar superíndices, como en la fórmula de la energía, utilizamos la etiqueta `<sup>`: **E**=**mc**<sup>2</sup>.
```

